# ApiEstructurasDemo

## Cómo ejecutar

1. Abrir una terminal dentro de esta carpeta.
2. Ejecutar:

```bash
dotnet run
```

3. Probar el GET:

```text
http://localhost:PUERTO/api/nodos
```

4. Probar el POST con JSON:

```json
{
  "id": 15,
  "valor": "Nuevo Nodo Derecho"
}
```
